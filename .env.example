# ============================================
# Meu Projecto — Variáveis de Ambiente
# ============================================

# --- App ---
APP_PORT=8080

# --- PostgreSQL ---
POSTGRES_DB=meu_projecto_db
POSTGRES_USER=meu_projecto_user
# Password em secrets/db_password (Docker secrets)

# --- Redis ---
# Password em secrets/redis_password (Docker secrets)

# --- Node / Vite ---
VITE_PORT=5173

# --- Mailpit ---
MAILPIT_PORT=8025
